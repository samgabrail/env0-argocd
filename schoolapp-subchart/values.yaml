# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

mongodb:
  auth:
    enabled: true
    rootUser: 'schoolapp'
    rootPassword: 'mongoRootPass1'

schoolapp-api:
  vault:
    status: 'disabled'
    kind: 'disabled'